# BeePos-App UI preview

The preview below documents the POS terminal layout committed with the project. It is an SVG design preview generated from the current UI structure and can be opened directly in a browser or image viewer.

- Preview: [`pos-terminal.svg`](./pos-terminal.svg)
- Live UI source: [`frontend/src/App.tsx`](../frontend/src/App.tsx)

A browser-rendered PNG screenshot should be generated after running the frontend locally with `npm run dev`, because this environment does not provide a browser capture tool.
